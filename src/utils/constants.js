export const SUBDOMAIN_REGEX = /^[a-zA-Z0-9._%+-]+@(.*\.)?edu$/;

export const EMAIL_REGEX = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
export const PASSWORD_MIN_LENGTH = 8;

export const EMAIL_PLACEHOLDER = "info@z.academy";
export const USERNAME_PLACEHOLDER = "Admin";

export const WEB_APP_NAME = "Z Academy";